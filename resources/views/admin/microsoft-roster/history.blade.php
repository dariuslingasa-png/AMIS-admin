<x-admin-layout title="Microsoft Sync History">
    <div class="mb-6"><p class="mb-1 text-[10px] font-black uppercase tracking-widest text-blue-700">Microsoft Integration</p><h1 class="text-2xl font-extrabold text-slate-950">Sync History</h1><p class="mt-1 text-sm text-slate-500">Read-only execution summaries for full and individual Team synchronization.</p></div>
    <x-card title="Synchronization Runs"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-[10px] font-black uppercase tracking-wider text-slate-500"><tr><th class="px-4 py-3">Date</th><th class="px-4 py-3">Sync Type</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Started By</th><th class="px-4 py-3">Teams Processed</th><th class="px-4 py-3">Members Found</th><th class="px-4 py-3">Matched</th><th class="px-4 py-3">Unmatched</th><th class="px-4 py-3">New / Removed</th><th class="px-4 py-3">Duration</th><th class="px-4 py-3">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($runs as $run)<tr><td class="px-4 py-3 text-xs">{{ ($run->started_at ?? $run->created_at)->format('M j, Y g:i A') }}</td><td class="px-4 py-3 capitalize">{{ $run->sync_type }}</td><td class="px-4 py-3"><span class="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-black uppercase">{{ str_replace('_',' ',$run->status) }}</span></td><td class="px-4 py-3">{{ $run->startedBy?->name ?? 'CLI / System' }}</td><td class="px-4 py-3 font-bold">{{ $run->teams_processed }} / {{ $run->teams_discovered }}</td><td class="px-4 py-3">{{ $run->members_discovered }}</td><td class="px-4 py-3">{{ $run->matched_students + $run->matched_faculty }}</td><td class="px-4 py-3">{{ $run->unmatched_accounts }}</td><td class="px-4 py-3">{{ $run->new_memberships }} / {{ $run->removed_memberships }}</td><td class="px-4 py-3">{{ $run->duration !== null ? $run->duration.'s' : '—' }}</td><td class="px-4 py-3"><a href="{{ route('admin.microsoft-roster.history.show',$run) }}" class="font-bold text-blue-700">{{ $run->error_summary ? 'View Errors' : 'View Summary' }}</a></td></tr>@empty<tr><td colspan="11" class="px-4 py-12 text-center text-slate-500">No synchronization runs have been recorded.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $runs->links() }}</div></x-card>
</x-admin-layout>
