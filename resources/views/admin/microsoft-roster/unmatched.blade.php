<x-admin-layout title="Unmatched Microsoft Accounts">
    @include('admin.microsoft-roster._alerts')
    <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p class="mb-1 text-[10px] font-black uppercase tracking-widest text-blue-700">Microsoft Integration</p><h1 class="text-2xl font-extrabold text-slate-950">Unmatched Accounts</h1><p class="mt-1 text-sm text-slate-500">Review Microsoft Team members that have no safe exact AMIS match.</p></div><div class="flex flex-wrap gap-2"><form method="GET" class="flex gap-2"><input name="search" value="{{ request('search') }}" placeholder="Name or Microsoft email" class="rounded-xl border-slate-200 text-sm"><button class="rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white">Search</button></form><a href="{{ route('admin.microsoft-roster.unmatched.export','csv') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm font-bold">CSV</a><a href="{{ route('admin.microsoft-roster.unmatched.export','json') }}" class="rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm font-bold">JSON</a></div></div>
    <div class="mb-5 rounded-xl border border-blue-200 bg-blue-50 px-4 py-3 text-sm font-semibold text-blue-900">Display-name similarity is never auto-confirmed. Use manual review and verify the selected AMIS record carefully.</div>
    <x-card title="Manual Review Queue" subtitle="Grouped by stable Microsoft identity"><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="bg-slate-50 text-[10px] font-black uppercase tracking-wider text-slate-500"><tr><th class="px-4 py-3">Microsoft Name</th><th class="px-4 py-3">Microsoft Email</th><th class="px-4 py-3">Teams Joined</th><th class="px-4 py-3">Possible AMIS Record</th><th class="px-4 py-3">Status</th><th class="px-4 py-3">Last Seen</th><th class="px-4 py-3">Actions</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($accounts as $account)<tr><td class="px-4 py-3 font-bold">{{ $account->display_name }}</td><td class="px-4 py-3">{{ $account->email ?? $account->user_principal_name ?? '—' }}</td><td class="px-4 py-3 font-bold">{{ $account->teams_joined }}</td><td class="px-4 py-3 text-xs text-slate-500">Manual search required</td><td class="px-4 py-3"><span class="rounded-full bg-rose-50 px-2 py-1 text-[10px] font-black uppercase text-rose-700">{{ str_replace('_',' ',$account->match_status) }}</span></td><td class="px-4 py-3 text-xs text-slate-500">{{ $account->last_seen_at ? \Illuminate\Support\Carbon::parse($account->last_seen_at)->diffForHumans() : '—' }}</td><td class="px-4 py-3"><div class="flex flex-wrap gap-2"><a class="font-bold text-blue-700" href="{{ route('admin.microsoft-roster.matches.review',$account->id) }}">Find / Match</a><form method="POST" action="{{ route('admin.microsoft-roster.matches.ignore',$account->id) }}">@csrf<button class="font-bold text-slate-600" onclick="return confirm('Ignore this account across all synchronized Teams?')">Ignore</button></form></div></td></tr>@empty<tr><td colspan="7" class="px-4 py-12 text-center text-slate-500">No unmatched active accounts. Great.</td></tr>@endforelse</tbody></table></div><div class="p-4">{{ $accounts->links() }}</div></x-card>
</x-admin-layout>
